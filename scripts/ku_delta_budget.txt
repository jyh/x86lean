# ARM A′'s OWN REGISTRY — the ku floor, and the absolute ms ceilings that are
# ARM A's complement.  Read by `scripts/ku_delta.py`; see its module docstring for
# the design and the helm's ruling of 2026-09-13.
#
# ⛔⛔ THE ku BUDGETS ARE NOT IN THIS FILE, AND THAT IS DELIBERATE.  A module's Δku
# allowance is its registered RELATIVE ms budget from
# `scripts/kernel_delta_budget.txt`, applied to the module's base ku MEASURED IN
# THE SAME RUN.  Nothing is copied here and no base reading is stored, so there is
# no number in this file that can go stale against that one.
#
# ⚠️ THAT TRANSFER IS A POLICY DECISION, DECLARED, AND NOT A MEASUREMENT.  Those
# percentages were derived as NOISE allowances for a CLOCK.  Applied to a counter
# whose noise is exactly zero (D187: Δku 0 on all five no-op commits, identical on
# two machines) they are LOOSE — and the direction is stated because loose is the
# unpoliced direction.  What would tighten them is a Δku walk over real commits
# PER MODULE, and no such corpus exists: every ku corpus in `docs/` is
# `Tests.Coverage` and nothing else (12 commits x 1 module).  That is D229 §4's
# finding one level down, it is the OPEN ITEM against this file, and it is cheap
# now that a ku reading is SECONDS (X86.Basic 1.8 s, X86.Syntax 3.2 s, measured
# 2026-09-13) rather than the minutes the ms walk costs.

# ── THE ku FLOOR ────────────────────────────────────────────────────────────
# A minimum absolute allowance in UNFOLDINGS, for the same reason the ms budget
# file carries one: a percentage of a small base is not an allowance.
#
# ⛔ AND HERE IT IS NOT A NOISE FLOOR, BECAUSE THERE IS NO NOISE.  The ms file's
# `@floor 6` is "the resolution this box has on a module a percentage cannot
# express".  This instrument's resolution is EXACTLY ZERO — D228's control read
# the same file twice at Δ 0, and D229's constructor plant read Δ 0 twice at N=0.
# So this number is not derived from a spread; it is a POLICY MINIMUM and it is
# labelled as one rather than dressed in a measurement.
#
# The value is the one measured small quantity in this system: the ku instrument's
# own header costs 10 unfoldings (`ku_module_census.py` asserts it every run, and
# refuses if a module's wrapped total is not its unwrapped total plus exactly
# this).  It is the smallest quantity the instrument ever attributes to anything.
#
# ⛔ AND THE CONSEQUENCE I WROTE HERE BEFORE MEASURING IT WAS WRONG, SO IT IS
# CORRECTED RATHER THAN QUIETLY DELETED.  This paragraph read: *"`Tests.VectorRuns`
# has a base ku of 0, so its allowance is this floor and nothing more — the
# tightest cell in the registry."*  **The gate's first real run says its allowance
# is 12, not 10, and the floor does not bind there at all.**  The census reports
# that module's UNWRAPPED ku as 0, but the quantity this gate differences is the
# WRAPPED one, which is 10 — the header — so its budget is 116.9% x 10 = 11.7, just
# above the floor.  ⇒ 🔑 AN EXPLANATION WRITTEN BEFORE THE NUMBER ARRIVES FITS
# WHATEVER ARRIVES, and nothing downstream would ever have tested this one.
# [[feedback-prose-written-before-the-measurement]]
# ⚠️ THE POINT SURVIVES ITS WRONG ARITHMETIC AND IS WORTH KEEPING: measured on the
# first full run, `Tests.VectorRuns` (allowance 12) and `X86.Flags` (33) are the
# two tightest cells in the registry by two orders of magnitude, and
# `Tests.VectorRuns` is GENERATED every batch.  If a batch trips it the first
# reading is INFORMATION — it would be the first evidence that generated vector
# rows carry kernel unfoldings at all — and the answer is to read it, not to raise
# this number.  A gate relaxed on schedule is not a gate (this repository's own
# law, in `kernel_ceilings.txt`'s header).
@floor 10

# ── THE ABSOLUTE ms CEILINGS ────────────────────────────────────────────────
# `<module> @ms @on <machine> <ms>`.  Three modules, because D229 §1 measured
# these three as the ones whose ms/ku sits OUTSIDE D228's unfolding band (1.5x,
# 2.9x and 3.5x the band top) — 906 of their 1,328 ms is time the unfolding
# constant does not account for.  They are also the modules a P2 proof or an
# enum-growing batch changes.
#
# ⛔ A CEILING WITH NO `@on` IS ABSENT, NEVER A LOOSE BOUND (the helm's D198 rule
# for the other registry, and it binds here for the same reason: the local<->runner
# factor is per module, measured 1.6x-2.4x, and cannot be divided out of an
# absolute number).  ⚠️ UNTIL 2026-09-14 THE RUNNER HAD NO ENTRY AND A′ REFUSED THERE,
# by design and loudly; its `ku-delta` job printed the runner's own readings every
# run, and those readings are what the runner block at the end of this file was
# registered from (ARMA-1, D244) — exactly how `X86.Program` and `Tests.Program` got
# theirs in `kernel_ceilings.txt`.
#
# ⭐ DERIVED BY THE RULE `kernel_ceilings.txt`'s OWN HEADER STATES FOR ITS ABSOLUTE
# LINES — `max(measured x 3.0, 50ms)` — on the WORST of four readings, which is
# that file's convention too.  The multiple is NOT invented here to fit: an
# allowance must not come from an argument about the thing it checks.
#
#     module          four readings (kernel_cost.py --emit-json, 2026-09-13)   worst   x3
#     X86.Basic        83.3 ·  92.5 ·  87.9 ·  90.1                             92.5    278
#     X86.Syntax      271.0 · 293.0 · 270.0 · 281.0                            293.0    879
#     X86.Theorems    970.0 · 1110.0 · 1010.0 · 1110.0                        1110.0   3330
#
# ⚠️ CONDITIONS, because a figure without them cannot be compared: one-minute loads
# 6.45 / 9.82 / 9.13 across the last three passes, WELL ABOVE the 2.2-4.1 band
# `kernel_cost.py` measured as having no effect.  These readings are therefore on
# the HIGH side and these ceilings are on the LOOSE side of where a quiet
# measurement would put them.  That is the SAFE direction for a tripwire — it
# cannot fire spuriously — and it is stated rather than left to be inferred.  A
# quiet re-measurement can tighten all three.
#
# ⛔⛔ AND WHAT THE FOUR READINGS SAY ABOUT THE OTHER REGISTRY, WHICH NOTHING WAS
# LOOKING AT: `kernel_ceilings.txt` carries `X86.Syntax 200` and `X86.Theorems
# 1100`, and this tree measures 270-293 and 970-1110.  TWO OF THE THREE ARE
# ALREADY OVER THEIR REGISTERED READING, X86.Syntax by up to 47%.  Those lines were
# retired as a merge gate by the helm on 2026-09-04 and kept as READINGS, and a
# reading nobody reads is not retired, it is abandoned.  Recorded here rather than
# repaired here: raising them is a change to the OTHER gate's file and belongs in
# its own commit with its own control.
#
# ⭐ THESE NUMBERS ARE COARSE ON PURPOSE AND MUST NOT BE TIGHTENED TOWARDS x1.6.
# That file's header is a chronicle of what x1.6 becomes: `X86.Syntax` passing at
# 97.6%, `vectorCoverage` at 98% and reporting the machine rather than the code.
# At x3 the margin is far outside this box's measured 4-8% contention band, so this
# half CANNOT fire on load.  It catches SIZE — which is the one thing ku provably
# cannot see (D228: literal arithmetic and large no-unfolding terms, unbounded
# kernel ms at Δku 0) and the one thing that grows fastest where ku falls behind
# (D229 §2: 1.8x at 128 constructors, 3.6x at 512).
X86.Basic @ms @on yukon.lan 278
X86.Syntax @ms @on yukon.lan 879
X86.Theorems @ms @on yukon.lan 3330

# ── THE RUNNER (ARMA-1, D244) ───────────────────────────────────────────────
# Registered 2026-09-14 by the rule above — max(worst x 3.0, 50 ms) — from the
# `READINGS on runnervmlun5p` block of EVERY completed `ku-delta` job since the job
# existed: 16 runs (push and pull_request), bd8fa81 .. 2f612fb, a window in which NO
# Lean input changed, so all 16 are readings of one tree.
#
#     module          16 runner readings (ms, one pass each)                         worst   x3
#     X86.Basic       171 173 175 157 173 166 173 141 161 178 99 189 169 181 178 166       189    567
#     X86.Syntax      513 513 509 547 556 548 537 459 546 534 328 536 511 541 531 528       556   1668
#     X86.Theorems   1920 1870 1940 1720 2010 1630 2070 1650 1680 2100 1140 1990 1890 2220 1990 1860  2220   6660
#
# ⛔ THE WORST OF SIXTEEN, NOT OF FOUR — and not a sample picked after reading it: the
#   population is every job that exists, and a worst over more readings can only be
#   higher than the worst of any four of them, which is the safe direction for a
#   tripwire. [[feedback-a-sample-chosen-after-its-outcomes]]
# ⛔⛔ ONE MACHINE NAME IS NOT ONE MACHINE (D243). The 11th column is a FAST VM draw
#   (aa4e7777's push run): 0.58x the others on all three modules at once. A ceiling
#   is judged against every VM class GitHub hands this job, so the margin that
#   matters is worst x3 over the SLOWEST class seen: a draw would have to be 3x
#   slower than the slowest of sixteen to red on load alone.
# ⚠️ AND IT CUTS THE OTHER WAY FOR THE MEASURED RED-FIRST ARM: a plant must clear
#   these ceilings on the FASTEST draw, which is why K3_N moved 4000 -> 8000.
X86.Basic @ms @on runnervmlun5p 567
X86.Syntax @ms @on runnervmlun5p 1668
X86.Theorems @ms @on runnervmlun5p 6660

# ── NEW-MODULE REGISTRATIONS (D252) ─────────────────────────────────────────
# `<module> @ku <n>`: the absolute unfoldings a module was accepted at IN THE STEP
# THAT ADDED IT. Consulted only for a module the measurement records as absent from
# the base tree (`new_modules`); from the next step on the module has a base and
# its relative budget applies, so a line here is dead after its landing and says so
# by being dated. No `@on` (ku is machine-independent, D185) and no headroom (ku is
# exact: the registration IS the reading, and accepting it is the decision).
# ⛔ WHY THIS BLOCK EXISTS: until D252 a new module was priced at `@floor`, 10
# unfoldings, whatever it was — D192's defect, repaired in the ms gate on 2026-09-10
# and never carried into A′. P2 batch 32 measured it: `X86.SoftFloat` +221 against 10.
#
# X86.SoftFloat — P2 batch 32 (D140), landed 2026-09-15. 221 unfoldings, read by
# `ku_delta.py --arm a-prime` on 53c4823 -> 327622b (yukon.lan, 2026-09-15). The
# BitVec IEEE-754 comparison `fcmp` and its two formats. ⛔ This comment said "the 818-case kernel
# differential that validates it lives in the same module" until 2026-09-15: the module has no theorem,
# and that run was never kept (D254). The kernel checks now live in `Tests.Anchors` (`fcmp_ieee_*`,
# `minmax_ieee_*`); P2 batch 38 added `fmin`/`fmax` here at Δku 0 (D253 §1).
X86.SoftFloat @ku 221
